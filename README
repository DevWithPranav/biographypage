# 🔖 Task 2: Styling a Biography Page 🌟

This is a submission for **Task 2** from the muLearn Web Development track.

## 📄 About the Task

In this task, we were asked to build and style a simple **Biography Page** using **HTML** and **CSS**, based on the instructions from the MDN CSS course.

🔗 Reference: [MDN CSS First Steps](https://developer.mozilla.org/en-US/docs/Learn/CSS/First_steps)

## 🎯 What I Did

- Created a basic bio page with HTML
- Styled it using CSS as per the challenge

### ✅ Styling Instructions Followed:

- Made the `<h1>` heading `hotpink` and gave it a `10px dotted purple` bottom border
- Made the `<h2>` heading *italic*
- Styled the contact details `<ul>` with:
  - Light grey background (`#eeeeee`)
  - Solid purple border (`5px`)
  - Padding inside the box
- Made links change to **green** on hover
